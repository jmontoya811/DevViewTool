require "spec_helper"

RSpec.describe DevViewTool do
  it "has a version number" do
    expect(DevViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
